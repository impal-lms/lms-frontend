{
  "name": "lms-frontend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "repository": "https://github.com/impal-lms/lms-frontend.git",
  "author": "blinfoldking",
  "license": "MIT",
  "private": false,
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc && mkdir -p dist/views && cp src/views/* dist/views/",
    "prestart": "npm run build",
    "start": "node . | pino-pretty",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "ejs": "^2.7.1",
    "express": "^4.17.1",
    "express-pino-logger": "^4.0.0",
    "morgan": "^1.9.1",
    "pino": "^5.13.5",
    "pino-pretty": "^3.2.2",
    "tslint": "^5.20.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/express-pino-logger": "^4.0.2",
    "@types/morgan": "^1.7.37",
    "@types/node": "^12.11.7",
    "@types/pino": "^5.8.12",
    "typescript": "^3.6.4"
  }
}
